<p id="notice"><%= notice %></p>

<h1>Individuals</h1>

<table>
  <thead>
    <tr>
      <th>Hyper</th>
      <th>Photo</th>
      <th>Linkd</th>
      <th>Player</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @individuals.each do |individual| %>
      <tr>
        <td><%= individual.hyper %></td>
        <td><%= individual.photo %></td>
        <td><%= individual.linkd %></td>
        <td><%= individual.player %></td>
        <td><%= link_to 'Show', individual %></td>
        <td><%= link_to 'Edit', edit_individual_path(individual) %></td>
        <td><%= link_to 'Destroy', individual, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Individual', new_individual_path %>
